// import { navItems, type NavItem } from '../data/NavItems';

// export function getNavItemsWithActive(pathname: string): (NavItem & { active: boolean })[] {
//   return navItems.map(item => ({
//     ...item,
//     active: item.href === pathname
//   }));
// }
